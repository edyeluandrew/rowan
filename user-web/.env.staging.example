# Rowan user-web — STAGING (phase-2/foundation)
# Copy to user-web/.env for local dev, or set in Vercel Preview / staging project

VITE_API_URL=https://rowan-backend-staging.onrender.com
VITE_STELLAR_HORIZON_URL=https://horizon-testnet.stellar.org
VITE_STELLAR_NETWORK=testnet

# Local dev against staging backend:
# VITE_API_URL=http://localhost:4000
