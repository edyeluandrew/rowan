/** Bridge: re-export shared formatters */
export {
  formatCurrency,
  formatXlm,
  formatUsdc,
  formatRate,
  formatDate,
  formatDateTime,
  formatTimeAgo,
  formatAddress,
} from '../../shared/utils/format';
